name: AI Agent

on:
  workflow_run:
    workflows: ["Task Runner"]
    types: [completed]

jobs:
  ai-agent:
    runs-on: ubuntu-latest
    steps:
      - name: Run AI
        run: |
          echo "Running AI logic based on completed tasks"