name: Task Runner

on:
  push:
    paths:
      - docs/Task.md

jobs:
  task-runner:
    runs-on: ubuntu-latest
    steps:
      - name: Execute Task
        run: |
          echo "Executing task from Task.md"