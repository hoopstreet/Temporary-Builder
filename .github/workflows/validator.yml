name: Validator

on:
  push:
    paths:
      - docs/DNA.md

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Validate changes
        run: |
          # Logic to validate changes before merging.