name: Merge Intelligence

on:
  push:
    paths:
      - docs/DNA.md

jobs:
  merger:
    runs-on: ubuntu-latest
    steps:
      - name: Merge changes
        run: |
          echo "Merging changes into the system"