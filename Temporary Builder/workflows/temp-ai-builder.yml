name: TEMP AI BUILDER (SINGLE SOURCE OF TRUTH)

on:
  push:
    paths:
      - "convo.md"
      - "convo2.md"
      - "Temporary Builder/**"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run Builder
        run: node "Temporary Builder/runner.js"
